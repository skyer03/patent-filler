# M5 内网图像模板集

当前仓库没有真实内网截图，因此本目录不伪造像素模板，识别默认使用 OCR 和网页 profile。`manifest.json` 是模板集的版本边界；现场完成首轮“只识别”后，只能把经用户审核的稳定按钮/图标小截图放入本目录，再登记来源步骤和阈值。

模板只用于确认锚点，不得单独决定点击坐标，也不得包含身份证号、手机号、邮箱、证书全文或业务字段值。模板文件名和 profile 的版本一起进入现场日志，便于回滚。
