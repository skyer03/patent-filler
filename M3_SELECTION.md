# M3 自动化引擎 PoC 与选型记录

日期：2026-08-04  
结论：采用轻量自研执行器作为首版路线，保留 MaaFramework 的流水线思想，不在当前版本引入 MaaFramework 依赖。

## 验证范围

离线 `InMemoryPageAdapter` 覆盖以下动作，并对每一步执行后回读：

- 普通文本框、日期框、按可见文字选择的下拉框；
- 进入技术摘要编辑态后写入长文本并验证编辑态和值；
- 复选框只在目标状态不一致时点击；
- 权利人/发明人动态表格只新增，验证原有行顺序、行数和新增末行；
- 人员选择器要求查询结果恰好唯一；
- 按区块锚点滚动，而不是依赖固定滚轮次数；
- 既有附件只核验文件名、预览和下载入口；
- 保存、返回、提交和删除在执行前阻断；急停和失焦在下一动作前暂停。

运行方式：

```text
python -m app m3
python -m unittest discover -s tests -v
```

## 选型依据

| 方案 | M3 结论 |
|---|---|
| MaaFramework + Python 自定义动作 | 暂不采用。依赖、打包和许可证/分发复核成本较高；当前没有真实内网页面可做现场验证。 |
| 轻量执行器 | 采用。复用现有 profile、OCR、窗口绑定和安全模式；新增动作后端协议，离线测试无需浏览器，Windows 现场可注入 Win32 输入后端。 |

动作执行器不使用 DOM、浏览器扩展或网页脚本。`ScreenActionExecutor` 只消费当前截图识别出的局部坐标；页面状态、焦点和动作后的回读由上层 `AutomationEngine` 控制。

## 现场限制与后续入口

本阶段可以在本地模拟页验证动作状态机和安全边界，但无法代替公司内网 Edge 的 100%/125% 缩放、真实中文输入、窗口焦点和网站版本验收。M4/M5 应将截图观察器接入 `PageAdapter`，按约定分辨率矩阵运行只识别/单步测试，并继续保持保存前暂停。
