# M0 需求与样本基线

生成日期：2026-08-04

本轮基线包含 50 份证书（发明 18 份、实用新型 32 份）。所有关键字段均已从证书文本层提取，发明人和权利人按分号切分，保留原始名单以支持复核。

本目录中的 `golden/` 是 M1 回归的结构化标准答案；`web_field_map.md`、`page_profile.md`、`permission_evidence.md` 与 `business_open_questions.md` 分别记录已核验的网页映射、权限更新后的页面锚点/可编辑控件、截图来源与观察结果和后续待确认项。权限截图只更新页面操作证据，不改变证书 golden 数据。
